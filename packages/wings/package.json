{
  "name": "@sky-castle/wings",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.ts",
  "bin": {
    "wings": "src/cli.ts"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "wings": "ts-node --files src/cli.ts",
    "build:server": "esbuild src/server.ts --bundle --external:socket.io --platform=node --target=node10.4 --outfile=build/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@msgpack/msgpack": "^2.5.1",
    "commander": "^7.2.0",
    "reflect-metadata": "^0.1.13",
    "socket.io": "^4.0.0",
    "socket.io-client": "^4.0.0"
  },
  "devDependencies": {
    "ava": "^3.15.0",
    "esbuild": "^0.10.0",
    "ts-node": "^9.1.1"
  }
}
